node_modules
dist
dist-ssr
*.local

# Secrets — nothing in this repo may ever contain an API key (CLAUDE.md, rule 4)
.env
.env.*
*.key
*.pem
*.jks

.claude/settings.local.json

# Screenshot output from shot.mjs. The script is committed; the PNGs are not — they
# regenerate in seconds and would add a megabyte per phase to the repo.
shots

.DS_Store
*.log
npm-debug.log*
coverage
.vscode/*
!.vscode/extensions.json
.idea
